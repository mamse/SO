#include <stdio.h>

int main () {
int a =10, b=3;
float r;
int v[4] = {2, 6, -9, 78};
r = (float)a / b;
printf("A divisão de %d por %d é %f\n", a, b, r );
int x = (b > 2);
printf("valor de x : %d\n", x);
}
